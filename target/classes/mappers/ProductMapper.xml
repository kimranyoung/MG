<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.MG.mapper.ProductMapper">

	<insert id="insert" parameterType="ProductDAO">
	insert into Product(proName, proPrice, proOrigin, proNumber, proBrand)
	values(#{proName}, #{proPrice}, #{proOrigin}, #{proNumber}, #{proBrand});
	</insert>
	
	
</mapper>